#include "sparseTimeSerie.hpp"

#include <iostream>

int main() {
    SparseTimeSerie<double> sts(50, 0);
    // sts(5, 5);
    // sts(10, 10);

    // std::cout<<sts<<std::endl;
    return EXIT_SUCCESS;
}